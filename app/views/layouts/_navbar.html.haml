%nav.navbar.navbar-default
	.container-fluid
		.navbar-header
			%button.navbar-toggle{"data-target" => "#myNavbar", "data-toggle" => "collapse", :type => "button"}
				%span.icon-bar
				%span.icon-bar
				%span.icon-bar
			%a.navbar-brand{:href => "#"} Shoff Accounting Client App
		#myNavbar.collapse.navbar-collapse
			%ul.nav.navbar-nav
				%li.nav-link
					= link_to "Sign In", new_user_session_path if !user_signed_in?
				%li.nav-link
					= link_to "Latest Financial Info", current_user if user_signed_in?
				%li.nav-link
					= link_to "FAQ", faq_path
				%li.nav-link
					= link_to "Sign Out", logout_path, :method => :delete if user_signed_in?